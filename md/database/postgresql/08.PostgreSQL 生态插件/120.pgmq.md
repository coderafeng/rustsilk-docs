# 概述

一个轻量级的消息队列。类似于 AWS SQS 和 RSMQ，但基于 Postgres。

- 轻量级 - 没有后台工作进程或外部依赖项，只有 Postgres SQL 对象
- 保证在可见性超时时间内向消费者“仅一次”送达消息
- 使用消息组键的先进先出 ( FIFO ) 队列进行有序处理
- 支持基于主题的路由，并可使用通配符模式实现发布/订阅和基于内容的路由
- 消息会一直保留在队列中，直到被明确删除。
- 为了长期保存和重放，消息可以被存档而不是删除。