# 概述

Sakila 示例数据库是使用最广泛的示例数据库之一，也是最好的示例数据库之一。Sakila 数据库为 DVD 租赁商店业务建模，其中包含影片、演员、影片与演员关系等内容，以及连接影片、商店和租赁业务的中央库存表。

Sakila 示例数据库最初来源于 MySQL，现在已经被移植到许多的数据库。

# 下载 Sakila 数据库

您可以从  https://github.com/jOOQ/sakila/tree/main/postgres-sakila-db 并下载以下两个文件：

1. postgres-sakila-schema.sql - 此文件包含了创建数据库结构的 sql 脚本，包含表，函数，存储过程，触发器等。
2. postgres-sakila-insert-data.sql - 此文件包含了导入数据的 sql 脚本。

这两个文件下载完成后，您要做的就是把这两个文件导入到数据库中。您可以使用以下两种工具中的任意一个将 sql 文件导入到数据库:

- psql 工具
- pgAdmin 工具
- Navicat 工具
- DBeaver 工具

# Sakila 示例数据库架构

Sakila 数据库包括了 16 个表，7 个视图，3 个存储过程，3 个存储函数，6 个触发器。

Sakila 数据库中的表：

- 演员表 **actor**: 演员的信息
- 地址表 **address**: 包含客户、员工和商店的地址信息
- 类别表 **category**: 影片的类别
- 城市表 **city**: 城市列表
- 国家表 **country**: 国家/地区列表
- 客户表 **customer**: 客户信息
- 影片演员关系表 **film_actor**: 影片和演员的多对多对应关系
- 影片类别关系表 **film_category**: 影片和类别的多对多对应关系
- 影片描述表 **film_text**: 影片的标题、描述信息
- 影片表 **film**: 所有影片
- 库存表 **inventory**: 影片拷贝的库存信息
- 语言表 **language**: 影片的语言信息
- 支付表 **payment**: 记录了客户的每笔付款
- 租赁表 **rental**: 包含每个库存物品的每次租赁明细
- 员工表 **staff**: 列出了所有员工的信息
- 商店表 **store**: 列出了系统中的所有商店

# Sakila 数据库结构

## 演员表

演员表 actor 表列出了所有演员的信息。

演员表 actor 表和影片表 film 通过 film_actor 表关联在一起。演员和影片是多对多的关系。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释                               |
|---:|---|---|---|---|------------------------------------|
| 1 | actor_id | integer | NO | nextval('actor_actor_id_seq'::regclass) | 用于唯一标识表中每个演员的代理主键 |
| 2 | first_name | character varying(45) | NO |  | 演员的名字                         |
| 3 | last_name | character varying(45) | NO |  | 演员姓                             |
| 4 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间       |

## 地址表

address 表包含客户、员工和商店的地址信息。

客户表 customer 中有外键引用 address 表的主键。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|-----:|---|---|---|---|---|
|    1 | address_id | integer | NO | nextval('address_address_id_seq'::regclass) | 代理主键，用于唯一标识表中的每个地址 |
|    2 | address | character varying(50) | NO |  | 地址的第一行 |
|    3 | address2 | character varying(50) | YES |  | 地址的可选第二行 |
|    4 | district | character varying(20) | NO |  | 地址所在的地区，可以是州、省、县等 |
|    5 | city_id | integer | NO |  | 外键，指向 city 表 |
|    6 | postal_code | character varying(10) | YES |  | 地址的邮政编码 |
|    7 | phone | character varying(20) | NO |  | 电话号码 |
|    8 | location | geometry | NO |  | 带有空间索引的几何数据 |
|    9 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 类别表

category 表列出了可以分配给影片的类别。

类别表和影片表 film 通过 film_category 表连接起来。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | category_id | integer | NO | nextval('category_category_id_seq'::regclass) | 用于唯一标识表中每个类别的代理主键 |
| 2 | name | character varying(25) | NO |  | 类别名称 |
| 3 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 城市表

city 表包含城市列表。

city 表中定义了外键指向 country 的主键。

address 表中定义了外键指向 city 表的逐渐。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | city_id | integer | NO | nextval('city_city_id_seq'::regclass) | 用于唯一标识表中每个城市的代理主键 |
| 2 | city | character varying(50) | NO |  | 城市名 |
| 3 | country_id | integer | NO |  | 标识城市所属国家的外键 |
| 4 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 国家表

country 表包含国家/地区列表。

country 表中定义了外键引用 city 表的主键。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | country_id | integer | NO | nextval('country_country_id_seq'::regclass) | 用于唯一标识表中每个国家/地区的代理主键 |
| 2 | country | character varying(50) | NO |  | 国家名 |
| 3 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 客户表

customer 表包含所有客户的列表。

customer 表中定义了外键引用 address 表和 store 表的主键。

payment 表和 rental 表引用了 customer 表的数据。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | customer_id | integer | NO | nextval('customer_customer_id_seq'::regclass) | 用于唯一标识表中每个客户的代理主键 |
| 2 | store_id | integer | NO |  | 标识客户“附近商店”的外键。顾客不仅从这家店租借，而且他们还在这家店购物 |
| 3 | first_name | character varying(45) | NO |  | 客户的名字 |
| 4 | last_name | character varying(45) | NO |  | 客户姓氏 |
| 5 | email | character varying(50) | YES |  | 客户电子邮件地址 |
| 6 | address_id | integer | NO |  | 标识 address 表中客户地址的外键  |
| 7 | active | boolean | NO | true | 表示客户是否为活跃客户。将此设置 FALSE 为可以替代物理删除客户。大多数查询应该带上 WHERE active = TRUE 子句 |
| 8 | create_date | date | NO | ('now'::text)::date | 客户加入系统的日期。此日期是在 `INSERT 时使用触发器自动设置的 |
| 9 | last_update | timestamp without time zone | YES | now() | 该行的创建时间或最近更新时间 |

## 影片演员关系表

film_actor 表用于支持影片和演员之间的多对多关系。

对于每个影片中的每个演员， film_actor 表中都对于应了一行数据。

film_actor 表使用外键引用 film 和 actor表。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | actor_id | integer | NO |  | 标识演员的外键 |
| 2 | film_id | integer | NO |  | 标识影片的外键 |
| 3 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 影片类别关系表

film_category 表用于支持影片和类别之间的多对多关系。对于应用于影片的每个类别，都对应了 film_category 表中的一行数据。

表使用外键 film_category 引用 film 和 category 表。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | film_id | integer | NO |  | 标识影片的外键 |
| 2 | category_id | integer | NO |  | 标识类别的外键 |
| 3 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 影片表

film 表列出了商店中可能有库存的所有影片。inventory 表中列出了每部影片的实际库存副本。

film 表中的语言字段指向了 language 表。

inventory 表中定义了指向 film 表的外键。

film_category 表中存储了影片和类别的多对多的关系。

film_actor 表中存储了影片和演员的多对多的关系。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释                                                         |
|---:|---|---|---|---|--------------------------------------------------------------|
| 1 | film_id | integer | NO | nextval('film_film_id_seq'::regclass) | 用于唯一标识表中每部影片的代理主键                           |
| 2 | title | character varying(255) | NO |  | 片名                                                         |
| 3 | description | text | YES |  | 影片的简短描述或情节摘要                                     |
| 4 | release_year | year | YES |  | 影片上映的年份                                               |
| 5 | language_id | integer | NO |  | 影片的语言。指向 language 表                                 |
| 6 | original_language_id | integer | YES |  | 影片的原始语言。当影片被配音成新语言时使用。指向 language 表 |
| 7 | rental_duration | smallint | NO | 3 | 租期的长度，以天为单位                                       |
| 8 | rental_rate | numeric(4,2) | NO | 4.99 | 在 rental_duration 字段中指定的时间段内租用影片的费用                                                             |
| 9 | length | smallint | YES |  | 影片的持续时间，以分钟为单位                                                             |
| 10 | replacement_cost | numeric(5,2) | NO | 19.99 | 如果影片未退回或以损坏状态退回，则向客户收取的费用                                                             |
| 11 | rating | mpaa_rating | YES | 'G'::mpaa_rating | 分配给影片的评级，可能的值：G， PG，PG-13， R， NC-17                                                             |
| 12 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间                                                             |
| 13 | special_features | text[] | YES |  | 列出 DVD 中包含的常见特殊功能。可以是零个或多个 Trailers, Commentaries, Deleted Scenes, Behind the Scenes                                                             |
| 14 | fulltext | tsvector | NO |  |                                                              |

## 库存表

inventory 表的一行数据代表了一个给定商店中一个给定影片的一个拷贝。

inventory 表中定义了外键指向 film 和 store 表。

租赁表 rental 中定义了外键指向 inventory 表。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | inventory_id | integer | NO | nextval('inventory_inventory_id_seq'::regclass) | 用于唯一标识库存中每个项目的代理主键 |
| 2 | film_id | integer | NO |  | 该项目所代表的影片主键 |
| 3 | store_id | integer | NO |  | 商店的主键 |
| 4 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 语言表

language 表列出了影片语言和原始语言所有可能的值。

language 表被影片表 film 引用 。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | language_id | integer | NO | nextval('language_language_id_seq'::regclass) | 用于唯一标识每种语言的代理主键 |
| 2 | name | character(20) | NO |  | 语言的英文名称 |
| 3 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 支付表

payment 表记录了客户的每笔付款，以及金额和所支付的租金等信息。

payment 表指向了客户表 customer，租赁表 rental 和 员工表 staff 表。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|-----:|---|---|---|---|---|
|    1 | payment_id | integer | NO | nextval('payment_payment_id_seq'::regclass) | 用于唯一标识每笔付款的代理主键 |
|    2 | customer_id | integer | NO |  | 正在应用其余额的客户。这是对 customer 表的外键引用 |
|    3 | staff_id | integer | NO |  | 处理付款的员工。这是对 staff 表的外键引用 |
|    4 | rental_id | integer | NO |  | 付款所适用的租金。这是可选的，因为有些付款是用于支付未付费用，可能与租金没有直接关系 |
|    5 | amount | numeric(5,2) | NO |  | 支付金额 |
|    6 | payment_date | timestamp without time zone | NO |  | 付款处理的日期 |
|    7 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 租赁表

rental 表包含每个库存物品的每次租赁的一行，其中包含有关谁租用什么物品、何时租用以及何时归还的信息。

rental 表定义了外键引用 inventory， customer 和 staff 表。

rental 表被 payment 中定义的外键引用。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | rental_id | integer | NO | nextval('rental_rental_id_seq'::regclass) | 唯一标识租赁的代理主键 |
| 2 | rental_date | timestamp without time zone | NO |  | 租借物品的日期和时间 |
| 3 | inventory_id | integer | NO |  | 正在出租的物品 |
| 4 | customer_id | integer | NO |  | 租用物品的客户 |
| 5 | return_date | timestamp without time zone | YES |  | 物品退回的日期和时间 |
| 6 | staff_id | integer | NO |  | 处理租赁的工作人员 |
| 7 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 员工表

staff 表列出了所有员工的信息，包括电子邮件地址、登录信息和图片。

staff 表中定义了外键引用 store 和 address 表。

staff 表被 rental， payment和 store 表中的外键引用。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释                                                                                   |
|---:|---|---|---|---|----------------------------------------------------------------------------------------|
| 1 | staff_id | integer | NO | nextval('staff_staff_id_seq'::regclass) | 唯一标识工作人员的代理主键                                                             |
| 2 | first_name | character varying(45) | NO |  | 工作人员的名字                                                                         |
| 3 | last_name | character varying(45) | NO |  | 工作人员的姓氏                                                                         |
| 4 | address_id | integer | NO |  | address 表中工作人员地址的外键                                                         |
| 5 | email | character varying(50) | YES |  | 工作人员的电子邮件地址                                                                 |
| 6 | store_id | integer | NO |  | 工作人员“家店”。”该员工可以在其他商店工作，但通常被分配到所列商店                      |
| 7 | active | boolean | NO | true | 这是否是一个活跃的员工。如果员工离开，他们的行不会从此表中删除；相反，此列设置为 FALSE |
| 8 | username | character varying(16) | NO | 工作人员用于访问租赁系统的用户名 |                                                                                        |
| 9 | password | character varying(40) | YES | 工作人员用于访问租赁系统的密码。密码应使用该 SHA2() 函数存储为散列 |                                                                                        |
| 10 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间                                                                                       |
| 11 | picture | bytea | YES |  | 包含员工照片的 BLOB                                                                    |

## 商店表

store 表列出了系统中的所有商店。所有库存都分配到特定的商店，员工和客户被分配到一个“附近的商店”。

store 表中定义了外键指向 staff 和 address 表。

store 表被 staff， customer 和 inventory 表中的外键引用。

| 序号 | 字段名 | 类型 | 允许空 | 默认值 | 注释 |
|---:|---|---|---|---|---|
| 1 | store_id | integer | NO | nextval('store_store_id_seq'::regclass) | 唯一标识商店的代理主键 |
| 2 | manager_staff_id | integer | NO |  | 标识该商店经理的外键 |
| 3 | address_id | integer | NO |  | 标识此商店地址的外键 |
| 4 | last_update | timestamp without time zone | NO | now() | 该行的创建时间或最近更新时间 |

## 视图

Sakila 数据库中共定义了 7 个视图：

1. actor_info
   actor_info 视图提供了所有演员的列表，包括他们所表演的影片，按类别细分。
2. customer_list
   customer_list 视图提供了一个客户列表，将客户额名字和姓氏连接在一起，地址信息合并到一个视图中。
   customer_list 视图结合了来自 customer ， address，city 和 country 表的数据。
3. film_list
   film_list 视图包含 film 表格的格式化视图，以及每部影片的以逗号分隔的演员列表。
   film_list 视图结合了 film ， category ， film_category，actor 和 film_actor 表的数据。
4. nicer_but_slower_film_list
   nicer_but_slower_film_list 视图包含 film 表格的格式化视图，以及以逗号分隔的影片演员列表。
   nicer_but_slower_film_list 视图与 film_list 演员列表中的视图不同。演员姓名的字母大写进行了调整，使每个姓名的第一个字母大写，而不是全大写。
   正如其名称所示， nicer_but_slower_film_list 视图执行额外的处理，因此返回数据的时间比 film_list 视图长。
   nicer_but_slower_film_list 视图视图结合了 film ， category ， film_category，actor 和 film_actor 表的数据。
5. sales_by_film_category
   sales_by_film_category 视图提供了单个影片类别汇总销售额的列表。
   由于一部影片可以列入多个类别，因此不建议通过合计此视图的行来计算总销售额。
   sales_by_film_category 视图结合了来自 category ， payment ，rental， inventory，film， film_category 和 category 表的数据。
6. sales_by_store
   sales_by_store 视图提供了按商店细分的总销售额列表。该视图返回商店位置、经理姓名和总销售额。
   sales_by_store 视图结合了 city ， country ， payment ，rental， inventory，store， address 和 staff 表的数据。
7. staff_list
   staff_list 视图提供了所有员工的列表，包括地址和商店信息。
   staff_list 视图包含来自 staff 和address 表的数据。

## 存储过程

Sakila 数据库中共定义了 3 个存储过程。

### film_in_stock

film_in_stock 存储过程确定一个给定的影片在给定的商店是否有库存。

#### 参数

- p_film_id
  要检查的影片的 ID，来自 film 表的 film_id 字段。
- p_store_id
  要检查的商店的 ID，来自 store 表的 store_id 字段。
- p_film_count
  OUT 返回库存影片拷贝数的参数。

#### 返回值

此过程生成库存影片副本的库存 ID 编号表，并返回（在 p_film_count 参数中）指示该表中的行数。

#### 示例用法

```sql
CALL film_in_stock(1,1,@count);
```

```text
+--------------+
| inventory_id |
+--------------+
|            1 |
|            2 |
|            3 |
|            4 |
+--------------+
4 rows in set (0.01 sec)

Query OK, 1 row affected (0.01 sec)
```

```sql
SELECT @count;
```

```text
+--------+
| @count |
+--------+
|      4 |
+--------+
1 row in set (0.00 sec)
```

### film_not_in_stock

film_not_in_stock 存储过程确定在给定的商店里一个给定的影片的任何拷贝是否没有库存（租出去）。

#### 参数

- p_film_id
  要检查的影片的 ID，来自 film 表的 film_id 字段。
- p_store_id
  要检查的商店的 ID，来自 store 表的 store_id 字段。
- p_film_count
  一个OUT 参数，返回没有库存的影片拷贝数。

#### 返回值

此过程为没有库存的影片副本生成一个库存 ID 列表，并返回（在 p_film_count 参数中）指示该表中的行数。

#### 示例用法

```sql
CALL film_not_in_stock(2, 2, @count);
```

```text
+————–—–—–—–—–—+ 
| inventory_id | 
+————–—–—–—–—–—+ 
|            9 | 
+————–—–—–—–—–—+ 
1 row in set (0.01 sec)

Query OK, 1 row affected (0.01 sec)
```

```sql
SELECT @count;
```

```text
+--------+
| @count |
+--------+
|      1 |
+--------+
1 row in set (0.00 sec)
```

### rewards_report

rewards_report 存储过程会生成一个由参数指定的上个月的优质客户的列表。

#### 参数

- min_monthly_purchases
  客户在上个月需要进行的最低购买或租赁次数才能获得资格。
- min_dollar_amount_purchased
  客户在上个月需要花费的最低金额才能获得资格。
- count_rewardees
  OUT 返回满足指定资格的客户计数的参数。

#### 返回值

此过程会生成符合指定资格的客户表。该表的结构与 customer 表相同。该过程还返回（在 count_rewardees 参数中）该表中的行数。

#### 示例用法

```sql
CALL rewards_report(7, 20.00, @count);
```

```text
...
| 598         | 1        | WADE        | DELVALLE     | WADE.DELVALLE@sakilacustomer.org         | 604        | 1      | 2006-02-24 10:48:30 | 2006-02-15 04:57:20 |
| 599         | 2        | AUSTIN      | CINTRON      | AUSTIN.CINTRON@sakilacustomer.org        | 605        | 1      | 2006-02-24 10:48:30 | 2006-02-15 04:57:20 |
...

42 rows in set (0.11 sec)

Query OK, 0 rows affected (0.67 sec)
```

```sql
SELECT @count;
```

```text
+--------+
| @count |
+--------+
|     42 |
+--------+
1 row in set (0.00 sec)
```

## 存储函数

Sakila 数据库中共定义了 3 个存储函数。


### get_customer_balance

get_customer_balance 函数返回指定客户帐户的当前的余额。

#### 参数

- p_customer_id
  要检查的客户的 ID，来自 customer 表的 customer_id。
- p_effective_date
  客户余额的截止日期。此日期之后的任何租金、付款等均不计算在内。

#### 返回值

此函数返回客户帐户的当前余额。

#### 示例用法

```sql
SELECT get_customer_balance(298,NOW());
```

```text
+---------------------------------+
| get_customer_balance(298,NOW()) |
+---------------------------------+
|                           22.00 |
+---------------------------------+
1 row in set (0.00 sec)
```

### inventory_held_by_customer

inventory_held_by_customer 函数返回正在租用指定库存项目的客户的 customer_id 。

#### 参数

- p_inventory_id
  要检查的库存项目的 ID。

#### 返回值

此函数返回当前正在租用该项目的客户的 customer_id。如果该项目未出租，返回 NULL。

#### 示例用法

```sql
SELECT inventory_held_by_customer(8);
```

```text
+-------------------------------+
| inventory_held_by_customer(8) |
+-------------------------------+
|                          NULL |
+-------------------------------+
1 row in set (0.00 sec)
```

```sql
SELECT inventory_held_by_customer(9);
```

```text
+-------------------------------+
| inventory_held_by_customer(9) |
+-------------------------------+
|                           366 |
+-------------------------------+
1 row in set (0.00 sec)
```

### inventory_in_stock

inventory_in_stock 函数返回一个布尔值，指示指定的库存项目是否有库存。

#### 参数

- p_inventory_id
  要检查的库存项目的 ID。

#### 返回值

此函数返回 TRUE 或 FALSE 以指示指定的项目是否有库存。

#### 示例用法

```sql
SELECT inventory_in_stock(9);
```

```text
+-----------------------+
| inventory_in_stock(9) |
+-----------------------+
|                     0 |
+-----------------------+
1 row in set (0.00 sec)
```

```sql
SELECT inventory_in_stock(8);
```

```text
+-----------------------+
| inventory_in_stock(8) |
+-----------------------+
|                     1 |
+-----------------------+
1 row in set (0.00 sec)
```

## 触发器

Sakila 数据库触发器共定义了 6 个触发器，分别在插入、更新、删除数据的时候自动进行一些操作。

### customer_create_date

当 customer 插入新行时， customer_create_date 触发器将 customer 表的 create_date 字段设置为当前时间和日期。

### payment_date

当 payment 插入新行时， payment_date 触发器将 payment 表的 payment_date 字段设置为当前时间和日期。

### rental_date

当 rental 表插入新行时， rental_date 触发器将 rental 表的 rental_date 字段设置为当前时间和日期。

### ins_film

当 film 表插入新行时，ins_film 触发器将相应的数据插入到 film_text 表。

### upd_film

当 film 表更新行数据时，upd_film 触发器将相应的更新 film_text 表中的数据。

### del_film

当 film 表删除行时，del_film 触发器将删除 film_text 表中相应的行。